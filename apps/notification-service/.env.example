# docker-compose 환경에서는 서비스명을 호스트로 사용
# 로컬 직접 실행 시 localhost로 변경

# Redis DB 1번 사용 (order-service와 분리)
REDIS_URL=redis://redis:6379/1
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_CONSUMER_GROUP=notification-service
LOG_LEVEL=INFO
